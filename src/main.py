from dotenv import load_dotenv
import os

def main():
    load_dotenv()
    print("Daily Study & News Companion is ready.")
    # En próximos pasos: orquestar news → calendar → finanzas → reporte → envío

if __name__ == "__main__":
    main()
